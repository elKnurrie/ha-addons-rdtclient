#include <tunables/global>

profile rdt-client flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities - be very permissive for container operations
  capability,

  # Network access
  network,

  # File access - very permissive for container
  /** rwkl,
  
  # Specific denials only for truly dangerous paths
  deny /proc/sys/kernel/core_pattern w,
  deny /proc/sysrq-trigger w,
  deny /proc/sys/kernel/hotplug w,
  deny /sys/kernel/security/** w,
}
