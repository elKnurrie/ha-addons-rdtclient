#include <tunables/global>

profile rdt-client flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Basic capabilities
  capability net_bind_service,
  capability chown,
  capability fowner,

  # Network access
  network inet stream,
  network inet6 stream,

  # File access
  /data/** rwk,
  /share/** rwk,
  /tmp/** rwk,
  
  # Application files
  /app/** rix,
  /usr/** rix,
  /bin/** rix,
  /lib/** rm,
  /lib64/** rm,
  
  # System files (read-only)
  /etc/** r,
  /proc/sys/kernel/random/uuid r,
  
  # Our custom scripts
  /usr/local/bin/ha-setup.sh rix,
  /custom-init rix,
  
  # Original init system
  /init rix,
  /package/** rix,
  /command/** rix,
  /run/s6/** rwk,
  
  # Deny sensitive paths
  deny /root/** rwklx,
  deny /sys/kernel/security/** rwklx,
}
