#include <tunables/global>

profile rdt-client flags=(attach_disconnected,mediate_deleted,complain) {
  #include <abstractions/base>

  # Allow everything - this is a very permissive profile for debugging
  /** rwklmx,
  
  # All capabilities
  capability,
  
  # All network access
  network,
  
  # Allow all signals
  signal,
  
  # Allow ptrace
  ptrace,
  
  # Allow mount operations
  mount,
  umount,
}
